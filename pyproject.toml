[project]
name = "dsic2706"
version = "0.1.0"
description = "Robust bioacoustic similarity retrieval under noise and domain shift"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
